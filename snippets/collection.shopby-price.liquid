<section class="widget woocommerce widget_product_categories filter-tag filter filter-price">
  <h3 class="filter-title" data-init="widgetToggle">{{section.settings.shop_by_price_title}}</h3>
  <div class="btn-filter-drop js-drop"><i class="ion-ios-arrow-down"></i></div>
                            <div class="clearfix"></div>
  <ul class="product-categories widget-inlineblock filter-content">
    {% assign tags = section.settings.shop_by_price_list_tags | split: ',' %}
    
    {% for t in tags %}
    {% assign tag = t | strip %}
    {% assign tag_value = tag | handleize %}
    <li class="cat-item{% if current_tags contains tag %} current-cat{% endif %}">
      <input type="checkbox" value="{{ tag_value | split: '|' | first }}" {% if current_tags contains tag %}checked{% endif %}/>
      <a href="javascript:void(0)" title="{{tag}}">{{tag}}</a>
    </li>
    {% endfor %}
  </ul>
  <!-- /.product-categories -->
</section>