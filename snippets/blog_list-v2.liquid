<div class="col-xs-12 col-sm-4 col-md-4 blog-item">
    <div class="post-info">
        <div class="post-date v2">{{article.published_at | time_tag: format: 'month_day_year'}}</div>
        <h3 class="post-title v2"><a href="{{ article.url }}">{{ article.title }}</a></h3>
        <div class="post-desc v2">{{article.content}}</div>
        <a href="{{ article.url }}" class="btn-find v2">FIND OUT MORE »</a>
    </div>
</div>